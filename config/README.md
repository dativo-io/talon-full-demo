# Talon configuration

Run `TALON_REPO=../talon ./scripts/bootstrap-talon-config.sh` to copy the current canonical three-agent product-demo baseline into gitignored `config/generated/`. This prevents silent schema drift.
